import React from 'react';
import ReactDOM from 'react-dom/client';
import { DeadlinePopupApp } from './DeadlinePopupApp';
import './styles.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <DeadlinePopupApp />
  </React.StrictMode>
);
